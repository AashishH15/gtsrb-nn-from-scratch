# Demo assets

Record a 30–60s screen capture of a training run and save it here as
`training-demo.mp4`:

- Windows Snipping Tool → record the terminal while `python compare_models.py`
  runs, or
- OBS / Xbox Game Bar

The README links to `docs/training-demo.mp4`. This file is a placeholder so the
folder is tracked until the clip is added.
